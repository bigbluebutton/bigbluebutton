PublishCollections = function() {

    ////////////////////////////////////////////////////////////////////
    // Publish
    //
    /*
    // publish Users
    Meteor.publish("Users", function () {
      return Orders.find();
    });

    // publish Meetings
    Meteor.publish("Meetings", function () {
      return Orders.find();
    });*/
}
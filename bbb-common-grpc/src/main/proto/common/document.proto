syntax = "proto3";

package org.bigbluebutton.protos;

message Document {
  // The name of the document.
  string name = 1;

  // Determines if this document should be the current one
  // in the meeting.
  bool current = 2;

  bool removable = 3;

  bool downloadable = 4;

  string url = 5;

  string file_name = 6;

  string content = 7;
}
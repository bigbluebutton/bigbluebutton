syntax = "proto3";

package org.bigbluebutton.protos;

import "presentation/UploadRequest.proto";
import "presentation/UploadResponse.proto";

service PresentationService {
  rpc Upload(UploadRequest) returns (UploadResponse);
}
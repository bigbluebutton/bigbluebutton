type = 'mobile'

srcDirs = ['src']

localeDir = 'locale'

locales = ['en_US', 'pt_BR']

additionalCompilerOptions = [
    "-target-player=11.2"
]

dependencies {
	merged project(':common-library')
	merged files("libs/as3-signals-utilities-async-v0.9-BETA.swc")
	merged files("libs/as3-signals-v0.9-BETA.swc")
	merged files("libs/flashassets.swc")
	merged files("libs/localelib.swc")
	merged files("libs/robotlegs-extensions-SignalCommandMap-v1.0.0b1.swc")
	merged files("libs/robotlegs-framework-v2.0.0.swc")
	
	merged files("libs/AirCapabilities.ane")
	merged files("libs/LocalNotificationLib.ane")
}

import React from 'react';
import { LdsRingDiv, LdsRingContainer,
} from './styles';

const Spinner = () => {
  return (
    <LdsRingContainer>
        <LdsRingDiv />
    </LdsRingContainer>
  );
};

export default Spinner;

import { check } from 'meteor/check';
import addShape from '../modifiers/addShape';

export default function handlePersistShape({ body }, meetingId) {
//   const { shape } = body;

  console.log("=================================================")
  console.log("HANDLING PERSIST SHAPE", JSON.stringify(shape))
  console.log("=================================================")


//   return addShape(shape);
}

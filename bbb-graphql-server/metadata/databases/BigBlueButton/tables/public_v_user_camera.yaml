table:
  name: v_user_camera
  schema: public
select_permissions:
  - role: bbb_client
    permission:
      columns:
        - meetingId
        - streamId
        - userId
      filter:
        meetingId:
          _eq: X-Hasura-MeetingId

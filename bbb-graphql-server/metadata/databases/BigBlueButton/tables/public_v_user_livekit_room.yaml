table:
  name: v_user_livekit_room
  schema: public
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: user_livekit_room
  custom_root_fields: {}
select_permissions:
  - role: bbb_client
    permission:
      columns:
        - roomName
        - purpose
        - token
      filter:
        _and:
          - meetingId:
              _eq: X-Hasura-MeetingId
          - userId:
              _eq: X-Hasura-UserId
  - role: bbb_client_not_in_meeting
    permission:
      columns:
        - roomName
        - purpose
        - token
      filter:
        _and:
          - meetingId:
              _eq: X-Hasura-MeetingId
          - userId:
              _eq: X-Hasura-UserId

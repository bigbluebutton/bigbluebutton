- name: allowed-queries
  definition:
    queries:
      - name: clientSettings
        query: |
          query clientStartupSettings {
            meeting_clientSettings {
              clientSettingsJson
            }
          }
      - name: userMetadata
        query: |
          query getUserMetadata {
            user_metadata {
              parameter
              value
            }
          }
